export type User = {
    name: string,
    lastName: string,
    number: number
}

export type Chat = {
    id: number,
    name: string,
    imageProfile: string, 
    message: string,
    date: string // change this
}

